import { defineConfig } from 'unocss'

export default defineConfig({
  // Add any custom configuration you need here
  shortcuts: [
    // For example, a shortcut for a common set of utility classes:
    ['btn', 'px-4 py-2 rounded bg-primary text-white hover:bg-primary-dark'],
  ],
  rules: [
    // Add custom rules if required
  ],
})
